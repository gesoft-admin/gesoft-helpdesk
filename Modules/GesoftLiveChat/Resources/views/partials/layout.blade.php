{{--
    A chat in Chat Mode reads like a chat: the newest message at the bottom and
    the reply editor under it.

    Nothing to see. operator.css turns the messages around when this is the
    first thing in the list, so the order is right from the first paint, and
    operator.js moves the editor below them.
--}}
<div class="gesoft-chat-layout" hidden></div>
